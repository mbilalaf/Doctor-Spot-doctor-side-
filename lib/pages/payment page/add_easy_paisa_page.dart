import 'package:flutter/material.dart';

class AddEasyPaisa extends StatelessWidget {
  const AddEasyPaisa({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('heloos'),
      ),
    );
  }
}
